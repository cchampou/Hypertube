
export const title = lang => (
    (lang === 'fr')?"Mon commentaire":"My comment"
)

export const fail = lang => (
    (lang === 'fr')?"Echec de la création du commentaire":"Fail to create your comment"    
)

export const ok = lang => (
    (lang === 'fr')?"Commentaire posté avec succès":"Comment successfully posted"    
)

export const submit = lang => (
    (lang === 'fr')?"Envoyer":"Send"
)