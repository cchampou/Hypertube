
export const title = lang => (
    (lang === 'fr')?"Définir un nouveau mot de passe":"Define a new password"
)

export const password = lang => (
    (lang === 'fr')?"Nouveau mot de passe":"New password"
)
export const confirmation = lang => (
    (lang === 'fr')?"Confirmation du nouveau mot de passe":"Confirm your new password"
)

export const submit = lang => (
    (lang === 'fr')?"Valider":"Submit"
)