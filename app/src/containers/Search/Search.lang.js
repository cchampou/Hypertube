
export const login = lang => (
    (lang === 'fr')?"Se connecter":"Log in"
)

export const signup = lang => (
    (lang === 'fr')?"Créer un compte":"Create an account"
)

export const slogan = lang => (
    (lang === 'fr')?"Toujours plus de films":"More and more movies"
)

export const seen = lang => (
    (lang === 'fr')?"Vu":"Seen"
)

export const notseen = lang => (
    (lang === 'fr')?"Non vu":"Not seen"
)

export const sort = lang => (
    (lang === 'fr')?"Trier par":"Sort by"
)

export const filterdate = lang => (
    (lang === 'fr')?"Filrer par date":"Filter by date"
)

export const filternote = lang => (
    (lang === 'fr')?"Filrer par note":"Filter by vote"
)

export const pop = lang => (
    (lang === 'fr')?"Popularité":"Popularity"
)

export const note = lang => (
    (lang === 'fr')?"Note":"Vote"
)

export const date = lang => (
    (lang === 'fr')?"Date de sortie":"Release date"
)

export const search = lang => (
    (lang === 'fr')?"Rechercher...":"Search..."
)

export const load = lang => (
    (lang === 'fr')?"Recherche des films disponibles ...":"Loading avalaible films ..."
)